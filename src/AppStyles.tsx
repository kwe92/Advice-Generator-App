import styled from "styled-components";
import { Row } from "./constants/styling/layout/Flex";

const AppContainer = styled(Row)`
  width: 100%;
  height: 100vh;
  align-items: center;
  justify-content: center;
`;

export default AppContainer;
